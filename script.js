const gridContainer = document.querySelector("#container");
const gridDiv = document.createElement("div");

gridDiv.textContent = "testing this stuff"
gridContainer.appendChild(gridDiv);
function makeGrid(){

};